" 显示git diff的插件
Plug 'airblade/vim-gitgutter',{'commit': '297678a'}
" -------------------------------------------------
" vim帮助中文版
Plug 'yianwillis/vimcdoc'
" -------------------------------------------------
"让Vim变成所见即所得的MarkDown文件编辑器，可以在编辑时预览最终效果。
" Plug 'iamcco/markdown-preview.vim'
" -------------------------------------------------
" show color, like red yellow #066
Plug 'gko/vim-coloresque'
" -------------------------------------------------
" 同时高亮多个单词 usage
Plug 'vim-scripts/Mark--Karkat'
nmap <unique> <silent> <Leader>n <Plug>MarkSearchAnyNext
" -------------------------------------------------
" 最大化当前panel.
Plug 'troydm/zoomwintab.vim'
" -------------------------------------------------
" 另一种对齐方式
Plug 'junegunn/vim-easy-align'
" -------------------------------------------------
" 排版
Plug 'rhysd/vim-clang-format'
" -------------------------------------------------
" end 自动补全
Plug 'tpope/vim-endwise'
" -------------------------------------------------
" gitk
Plug 'gregsexton/gitv'
" -------------------------------------------------
" 让书签可视化的插件
Plug 'kshenoy/vim-signature'
" -------------------------------------------------
" 自动调整窗口大小
" Plug 'justincampbell/vim-eighties'
" -------------------------------------------------
" Vim8 异步运行shell
Plug 'skywind3000/asyncrun.vim'
" -------------------------------------------------
" 查看和自动识别文件的编码格式
Plug 'mbbill/fencview'
" Plug 's3rvac/AutoFenc'
" -------------------------------------------------
" Screen-anchored cursor movement for Vim.
Plug 'reedes/vim-wheel'
" -------------------------------------------------
" A collection of language packs for Vim.
Plug 'sheerun/vim-polyglot'
" -------------------------------------------------
" 登陆到你选择的窗口，比如tmux的“显示面板”
Plug 't9md/vim-choosewin'
" -------------------------------------------------
" 非常简单的vim插件，方便调整您的vim窗口
Plug 'simeji/winresizer'
" -------------------------------------------------
Plug 'easymotion/vim-easymotion'
" -------------------------------------------------
" 总是为vim分割窗口有一个好的视图
Plug 'zhaocai/GoldenView.Vim'
" -------------------------------------------------
" VIM的撤销历史可视化工具
Plug 'mbbill/undotree'
" -------------------------------------------------
" 一个用于vim的树资源管理器插件
Plug 'scrooloose/nerdtree', { 'on':  ['NERDTreeToggle', 'NERDTreeTabsToggle'] }
" -------------------------------------------------

if !exists('g:evervim_no_restore_cursor')
    " 重新打开文件在您的最后编辑位置
    Plug 'farmergreg/vim-lastplace'
endif
" -------------------------------------------------
if exists('g:evervim_smooth_scrolling')
    " 为Vim世界带来基于物理的平滑滚动!
    Plug 'yuttie/comfortable-motion.vim'
endif
" -------------------------------------------------
" 非常棒的Git包装器
Plug 'tpope/vim-fugitive'
" -------------------------------------------------
Plug 'rhysd/conflict-marker.vim'
" -------------------------------------------------
" Gist的Vim插件
" Plug 'mattn/gist-vim'
" -------------------------------------------------
" Fuzzy finder choice
if exists('g:evervim_use_ctrlp') || WINDOWS()
    Plug 'ctrlpvim/ctrlp.vim'
    Plug 'tacahiroy/ctrlp-funky'
else
    Plug 'junegunn/fzf', { 'do': './install --bin' }
    Plug 'junegunn/fzf.vim'
endif
" -------------------------------------------------
" Make sure we run devicons after the above
if !TERMUX() && !exists('g:evervim_no_patched_fonts')
    Plug 'ryanoasis/vim-devicons'
endif
" -------------------------------------------------
Plug 'flazz/vim-colorschemes'
" -------------------------------------------------
" -------------------------------------------------
" -------------------------------------------------
" -------------------------------------------------
" -------------------------------------------------
" -------------------------------------------------

