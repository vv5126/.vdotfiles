# install module at user space.
python3.4 setup.py install --user
as python3.4 setup.py install --prefix=$HOME/.local

列表类似与C的结构体
有pop方法 append extend remove insert ...
列表分片
aaa[1:5]
in
not in



# easy_install install:
down setuptools-xx.x.x.zip from https://pypi.org/project/setuptools/#files
sudo python setup.py install

# pip install:
down pip-xx.x.x.tar.gz from https://pypi.org/project/pip/#files

# pip3 install:
sudo aptitude install python3-pip
