#!/bin/bash

LIBCONF="$HOME/.bin/base/lib-base.conf"

if [ "$#" != 0 ]; then
	[ -f "$1" ] && LIBCONF="$1"
	while read line; do
		if [ -f "$HOME/$line" ]; then
   			source "$HOME/$line"
#		else
#			echo skip "$HOME/$line"
		fi
	done < "$LIBCONF"
fi

