#!/bin/bash

source $HOME/.bin/base/base.conf

hostname="${HOSTNAME//-/_}"
list=($(echo ${!hostname}))

if [ -n $list ]; then
    for line in "${list[@]}"; do
        if [ -f "$HOME/$line" ]; then
            source "$HOME/$line"
        fi
    done
fi
