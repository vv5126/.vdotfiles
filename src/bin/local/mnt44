#!/bin/bash

local_dir=/home/user/4.4

[ -d $local_dir ] && {
	echo 'user' | sudo fusermount -u $local_dir && rm -rf $local_dir
} || {
	mkdir $local_dir
	echo 'user' | sudo sshfs wgao@192.168.4.4:/home1/wgao $local_dir -o uid=1000,gid=1000,allow_other || rm -rf $local_dir
}
unset local_dir
