#!/bin/bash

CLONER_LINK="$HOME/.local/cloner_link"

cd ~

cloner_file=$(ls ~/ -at1 | grep .cloner- | head -1 &2>/dev/null)

[ ! -f "$CLONER_LINK" ] && ln -s $HOME/$cloner_file/cloner $CLONER_LINK

cd $cloner_file

sudo $CLONER_LINK
