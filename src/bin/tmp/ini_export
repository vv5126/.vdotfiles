
function ini_export() {
	local val
	while read line; do
		if [[ "$line" =~ "VGLD" ]]; then
			val="${line%=*}"
			$line
			export $val
		elif [[ "$line" =~ "TOPATH" ]]; then
			val="${line##*=}"
			PATH=$val:$PATH
		fi
	done < $1
}


