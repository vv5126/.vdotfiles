#!/bin/bash

para=$*
src=${para% *}
dst=${para##* }

if [[ "$para" =~ "@" ]] && [[ "$para" =~ ":" ]]; then

	if [ $# -eq 2 ]; then
		[ -f $1 ] &&
	fi
	anymkdir $dst
	scp -r "$src" "$dst"
else
	cp -r "$src" "$dst"
fi
