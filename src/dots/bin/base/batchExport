#!/bin/bash

[[ $(ps -p $$ -o comm=) == 'zsh' ]] && HOSTNAME="$HOST"

source $HOME/.bin/base/base.conf

if [[ -n "$list" ]]; then
    for line in ${list[@]}; do
        if [ -f "$HOME/$line" ]; then
            source "$HOME/$line"
        fi
    done
fi
