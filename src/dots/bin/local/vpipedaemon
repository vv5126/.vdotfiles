#!/bin/bash

VGL_NOTIFY=/tmp/.vpipe
decode=/home/user/.bin/local/taskprocessor
tasklist=/tmp/vtask

[ ! -w "$VGL_NOTIFY" ] && mknod "$VGL_NOTIFY" pipe

while true; do
    str=$(cat $VGL_NOTIFY)
    echo -e "#!/bin/bash\n$str" > $tasklist
    $decode $tasklist
done

exit 0
