#!/bin/bash

	para_all="$*"

for para in $para_all; do
	if [ -d "$para" ]; then
		if [[ "$para" =~ "lib" ]]; then
			ls "$para" > .tmp
			while read line < .tmp; do
				source "$para/$line"
			done
			rm .tmp
		fi
	elif [ -f "$para" ]; then
		source "$para"
	fi
done
