#!/bin/sh

# kill指定进程
# e.x. /system/bin/surfaceflinger
function kill_process() {
    pid=$(adb shell ps | grep "$1" | awk '{print $2}')
    [ -n "$pid" ] && adb shell kill -9 $pid
}

kill_process $@
